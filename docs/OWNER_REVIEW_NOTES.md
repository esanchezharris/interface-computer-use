# Account-binding repair update

The demonstrated defect below has now been repaired; the earlier walkthrough is preserved as historical source analysis at `60a11c4ef51c8f99cffe53119e87aa312f18fac6`. Owner-operated handoff is now **passed twice**, based on your explicit personal verification and the linked original runs. Personal code review remains **pending**; generating notes or running the handoff does not complete code review.

The replacement [artifact](/Users/emiliosanchez-harris/interface-computer-use/artifacts/prepare-transfer.json:316) hashes to `4b0ffdc4c4510716f724d12e2ecc359c2b05d5bdfe98f699cf49ef3593c801e5`. Its `step-005` selects Source account using `sourceAccountRef`; [step-006](/Users/emiliosanchez-harris/interface-computer-use/artifacts/prepare-transfer.json:369) selects Destination account using `destinationAccountRef`. Both operations contain `kind: input`, `transform: identity` and matching `value-equals` postconditions. [Live events](/Users/emiliosanchez-harris/interface-computer-use/evidence/cf84d841-1d59-48cf-984a-0f8347b0c7c4/events.jsonl:20) record their real executions from current UI observations in non-default direction SAV-207 → CHK-207, 6100 cents.

The [narrow guard](/Users/emiliosanchez-harris/interface-computer-use/src/domain/account-bindings.ts:13) inspects completed recorded selections, their semantic roles, input names and result checks. [Recorder.artifact](/Users/emiliosanchez-harris/interface-computer-use/src/recording/recorder.ts:82) invokes the guard before emission. The model receives only completion feedback from those checked actions, plus live UI observations and the explicit binding requirement. The executor and final review verification are unchanged: they still select by requested option label and compare all extracted invocation values, fee validity, currency and review-only status. No account-pair allowlist or hidden host workflow was added.

[Parameterization tests](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/parameterization.test.ts:1) now verify all four directions/member combinations against the selected artifact bytes, plus a clearly labeled visible-DOM harness perturbation of defaults and option order. Replay uses a fatal model-import hook and has zero model calls and commits. [Binding regressions](/Users/emiliosanchez-harris/interface-computer-use/tests/unit/account-bindings.test.ts:1) reject missing/uncompleted/swapped bindings and missing result checks. [Discovery regression](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/discovery-bindings.test.ts:7) rejects a matching-default review without executed bindings. The old six-step artifact still fails; it was archived, never patched.

The repaired live bytes passed 37/37 qualification tests. [Repair record](/Users/emiliosanchez-harris/interface-computer-use/docs/ACCOUNT_REPAIR.md) contains current commands, results and evidence. The manual command remains `npm run demo:handoff -- --require-live`: expected review is M-207, CHK-207 → SAV-207, 37.50 USD, fee 0.00, AWAITING_CONFIRMATION, with terminal success and commits: 0. You supplied successful runs `e59f4654-727a-4f35-83fc-59fced282a63` and `78753464-d145-41f3-88b3-bc4cf3201efa` from clean revision `0369d808df669016564f98d3e2e1bc66e383f13d`. [Owner acceptance](/Users/emiliosanchez-harris/interface-computer-use/evidence/owner-handoff.json) preserves your attestation and terminal values separately from automatic manifests/events. Both records match the qualified hash, record human restoration/resumption, and report success with zero model calls/reserved tokens. The zero commits and exact result values come from your supplied terminal output; the saved safe result deliberately redacts those values. No fixture or test_operator run was relabeled.

---

# Historical walkthrough before the repair

The statements below describe the earlier six-step artifact and source, not current acceptance. Artifact links now point to the archived bytes; source line references below were checked at the original revision. The original document is also preserved byte-for-byte in commit `563d3951363a19679fd8ae13ab6f5c0f4b0740c7`.

# Owner review: 45-minute source walkthrough

Source inspected: `60a11c4ef51c8f99cffe53119e87aa312f18fac6`, branch `build/computer-use`.
The code and live artifact are unchanged. This document is preparation for your review, not evidence that you completed it. Personal code review and owner-operated handoff remain **pending**. No model API request, model comparison, new feature or general audit was performed for these notes.

Use the timed sections in order. Read the named function and its immediate callers, then answer the question aloud before checking the answer checkpoints. Historical tests below are source-inspected, not rerun in this session; the only new runtime work was the single account-direction check described first.

**0–9 minutes: resolve the account qualification question**

**Conclusion: the current six-step live artifact relies on default account selections.** The engine supports parameterized `select` actions, but this artifact has none. The account inputs affect final verification, not the controls selected during its execution. There is no enforced allowlist restricting the capability to the two example directions.

| Stage | Actual source / artifact | What the account inputs do |
|---|---|---|
| Invocation validation | [src/domain/contract.ts:4](/Users/emiliosanchez-harris/interface-computer-use/src/domain/contract.ts:4) and [src/replay/run.ts:20](/Users/emiliosanchez-harris/interface-computer-use/src/replay/run.ts:20) | Both references must match `(CHK|SAV)-three digits`, differ, and accompany a valid member ID and positive bounded integer amount. No checking-to-savings-only rule or member/pair allowlist exists. Validation does not establish account ownership. |
| Artifact contract | [artifacts/prepare-transfer.json:15](/Users/emiliosanchez-harris/interface-computer-use/artifacts/historical/prepare-transfer-294a274e.json:15); schema enforcement at [src/domain/artifact.ts:69](/Users/emiliosanchez-harris/interface-computer-use/src/domain/artifact.ts:69) | `contract.inputs.sourceAccountRef` and `destinationAccountRef` are declarations. They do not generate actions. The schema checks exact contract shape, versions, references and provenance; its refinement at line 98 does not require every input to drive an action. |
| Available parameter mechanism | [src/domain/actions.ts:17](/Users/emiliosanchez-harris/interface-computer-use/src/domain/actions.ts:17), [src/domain/actions.ts:36](/Users/emiliosanchez-harris/interface-computer-use/src/domain/actions.ts:36), [src/surface/executor.ts:59](/Users/emiliosanchez-harris/interface-computer-use/src/surface/executor.ts:59) | A `select` operation can carry `{kind:"input",name:"sourceAccountRef",transform:"identity"}`; the executor resolves it and calls `selectOption({label: ...})`. Destination works the same way. Host policy allows both account controls at [src/policy/profile.ts:66](/Users/emiliosanchez-harris/interface-computer-use/src/policy/profile.ts:66). |
| This artifact's bindings and steps | [artifacts/prepare-transfer.json:88](/Users/emiliosanchez-harris/interface-computer-use/artifacts/historical/prepare-transfer-294a274e.json:88), [artifacts/prepare-transfer.json:141](/Users/emiliosanchez-harris/interface-computer-use/artifacts/historical/prepare-transfer-294a274e.json:141) | Targets are Member ID, Search, Accounts, Prepare transfer, Amount USD, Review transfer. Only `steps[0].operation.value` and `steps[4].operation.value` read inputs: `memberId` and `amountCents`. There is no account combobox binding, `select`, or account-value precondition. |
| Current UI defaults | [sandbox/pages.ts:39](/Users/emiliosanchez-harris/interface-computer-use/sandbox/pages.ts:39) | Both dropdowns contain both member accounts. Source defaults to `member.accounts[0]`; destination to `[1]`. [sandbox/data.ts:42](/Users/emiliosanchez-harris/interface-computer-use/sandbox/data.ts:42) defines these as CHK then SAV for each member. Changing the member therefore changes the defaults, without either account input selecting anything. |
| Application-supported directions | [sandbox/server.ts:108](/Users/emiliosanchez-harris/interface-computer-use/sandbox/server.ts:108) | The review endpoint requires different existing accounts belonging to the member, a positive amount and enough source balance. It accepts either direction. It does not enforce CHK → SAV. This source inspection establishes what the synthetic application supports; the runner never imports this data. |
| Output verification | [src/surface/checks.ts:160](/Users/emiliosanchez-harris/interface-computer-use/src/surface/checks.ts:160) | Read unique visible review-table values, parse the output/money schema, then compare member, source, destination and amount to the invocation at lines 200–207. A wrong account causes `CHECKPOINT_MISMATCH`; success is not returned. |

The original examples at [BUILD_SPEC.md:61](/Users/emiliosanchez-harris/interface-computer-use/BUILD_SPEC.md:61) are examples, not an exclusive supported-pair contract. The existing tests at [tests/integration/outcomes.test.ts:9](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/outcomes.test.ts:9) request CHK-207 → SAV-207; `wrong-source` and `wrong-destination` fault cases deliberately corrupt review output. Those are strong output-rejection tests, not proof that the live artifact selects non-default accounts. The development fixture explicitly selects both accounts at [scripts/fixture-model.ts:21](/Users/emiliosanchez-harris/interface-computer-use/scripts/fixture-model.ts:21), but its behavior cannot be attributed to the genuine artifact.

**One bounded keyless check, actually performed.** Requested member M-207, **SAV-207 → CHK-207**, amount **3750 cents**. This is schema-valid and already supported by the UI/server: both options exist and the savings balance is 625000 cents. No supported-domain expansion or fixture change was needed.

A one-off stdin driver invoked `node --import ./dist/scripts/model-bomb.js --input-type=module` in a child with environment names containing `OPENAI`, `ANTHROPIC`, `API_KEY` or `CUA_` removed. It called the actual `replay()` with the unchanged live artifact, fresh `startSandbox({port:0})`, `headed:false`, `operator:false`, `actor:"automation"`, and 2000 ms action/condition limits. The outer process had a 45-second deadline. A passive `framenavigated` listener read the visible Source/Destination review cells; it did not select or alter anything. The driver checked the real server's counters separately from the runner.

| Observed fact | Result |
|---|---|
| InputSchema accepted the invocation | Yes |
| Displayed review direction | **CHK-207 → SAV-207**, the defaults |
| Actual replay result | **failed / CHECKPOINT_MISMATCH**, with no successful output object |
| Failed stage | Final output verification; `stepId:null` because all six recorded steps had completed |
| Independent review requests / commits | **1 / 0** |
| Model calls / reserved tokens | **0 / 0**, with fatal model-import hook active |
| Artifact SHA-256 before and after | `294a274e3a9cfc5a48bfecd0dc655c43de46c45934c95fffddb71b0a294d269d` |

Actual run: [.runs/owner-review/5606575e-f518-4ae9-8995-fbd1c77a42d1/manifest.json:1](/Users/emiliosanchez-harris/interface-computer-use/.runs/owner-review/5606575e-f518-4ae9-8995-fbd1c77a42d1/manifest.json:1). The source was clean when the check started. [.runs/owner-review/5606575e-f518-4ae9-8995-fbd1c77a42d1/events.jsonl:13](/Users/emiliosanchez-harris/interface-computer-use/.runs/owner-review/5606575e-f518-4ae9-8995-fbd1c77a42d1/events.jsonl:13) records step six completing, then the final mismatch; [.runs/owner-review/5606575e-f518-4ae9-8995-fbd1c77a42d1/result.safe.json:1](/Users/emiliosanchez-harris/interface-computer-use/.runs/owner-review/5606575e-f518-4ae9-8995-fbd1c77a42d1/result.safe.json:1) preserves the failed result. The automatic snapshot deliberately omits business values; the displayed direction and counters above are observations from the one-off checking driver, not invented fields in the saved manifest. The driver exited 0 because its assertions of the observed failure and safety behavior passed; the capability did **not** succeed. This was not a human handoff.

**Precise finding:** an accepted, application-supported account-direction input fails because the recorded procedure never consumes either account input for selection. The publication path at [src/discovery/run.ts:116](/Users/emiliosanchez-harris/interface-computer-use/src/discovery/run.ts:116) checks the current example's review and serializes the recorder; it does not establish account parameterization. Thus the earlier “permutations unqualified” statement was a coverage limitation, not an enforced contract restriction. This specific reverse direction is now **known to fail**. The historical 31/31 result remains true for its existing test cases; it did not cover this invocation. We do not infer additional defects solely from untested permutations.

**Smallest repair boundary, not implemented:** use the existing symbolic `select` mechanism for both account controls and require that coverage before discovery publishes a generally parameterized artifact. The model must actually choose and execute those selections, even when its initial example already has matching defaults; the recorder must preserve those real actions. A focused discovery-completion guard/feedback and one reverse-direction regression would prevent recurrence. Qualify a newly emitted artifact separately; preserve this one's bytes. Do not hand-edit this artifact, inject a hidden account workflow into replay, or treat the scripted fixture as the repair. New genuine discovery would require separately authorized future work; the paid phase remains closed. Merely relabeling the two examples as a supported-pair restriction would not implement such a restriction.

**Question:** Why did changing M-104/CHK-104/SAV-104 to M-207/CHK-207/SAV-207 pass, while swapping M-207's accounts fails?

Answer checkpoints: member input drives search; the app supplies member-specific CHK/SAV defaults; this artifact never selects accounts; final comparison detects the mismatch. Distinguish a safety check rejecting the wrong output from successful functional parameterization.

**9–17 minutes: one real discovery decision**

Follow **step-005, filling the nonsemantic Amount USD input**. Its historical request/response and completed action are at [evidence/fbcd21b6-5f82-4778-a73c-09afe5559dc7/events.jsonl:19](/Users/emiliosanchez-harris/interface-computer-use/evidence/fbcd21b6-5f82-4778-a73c-09afe5559dc7/events.jsonl:19) through line 22. The response is linked by its request ID; the event says `fill`, `table-label`, `step-005`. The recorded operation and verification conditions are at [artifacts/prepare-transfer.json:297](/Users/emiliosanchez-harris/interface-computer-use/artifacts/historical/prepare-transfer-294a274e.json:297). Raw provider JSON and transient candidate IDs were intentionally not persisted. The following is a trace of the accepted decision's semantics from code, artifact and events, not a purported verbatim transcript.

1. **Observe:** [src/surface/observe.ts:42](/Users/emiliosanchez-harris/interface-computer-use/src/surface/observe.ts:42) reads the live Workspace frame's visible DOM. It bounds controls to 80 and visible text to 10000 characters; password inputs are omitted. The amount input lacks a semantic label. Its enclosing row's first cell supplies `nearbyLabel`; lines 125–139 create a `table-label` binding and an observation-scoped candidate ID. The selected binding remains frame `Workspace`, role `textbox`, label `Amount USD`.
2. **Propose:** [src/discovery/run.ts:68](/Users/emiliosanchez-harris/interface-computer-use/src/discovery/run.ts:68) obtains that observation, captures the ownership epoch, and passes the goal, typed invocation and observation to `model.decide()` at line 87. [src/discovery/openai.ts:111](/Users/emiliosanchez-harris/interface-computer-use/src/discovery/openai.ts:111) packages the request with the Decision schema; the historical response led to a fill with input `amountCents`, transform `usd-decimal`. The request contained no fixture action sequence. Do not run this path during owner review.
3. **Validate:** [src/discovery/run.ts:100](/Users/emiliosanchez-harris/interface-computer-use/src/discovery/run.ts:100) discards a response if ownership changed, then `Decision.safeParse` checks its strict action/value schema. [src/recording/recorder.ts:31](/Users/emiliosanchez-harris/interface-computer-use/src/recording/recorder.ts:31) checks that its candidate belongs to the supplied observation, creates `target-005`, and constructs a tentative step. The candidate token is replaced by a persistent symbolic binding.
4. **Authorize and execute:** [src/surface/executor.ts:14](/Users/emiliosanchez-harris/interface-computer-use/src/surface/executor.ts:14) checks preconditions, resolves the current target, inspects its actual effect, and calls host `authorize` before dispatch. `uniqueTarget` at [src/surface/target.ts:23](/Users/emiliosanchez-harris/interface-computer-use/src/surface/target.ts:23) refuses zero/multiple/hidden/disabled/wrong-type controls and returns a pinned element. [src/policy/profile.ts:66](/Users/emiliosanchez-harris/interface-computer-use/src/policy/profile.ts:66) permits an amount fill. The executor rechecks ownership, then resolves integer cents into a decimal string and calls `fill` at line 55.
5. **Record only after verification:** [src/surface/executor.ts:84](/Users/emiliosanchez-harris/interface-computer-use/src/surface/executor.ts:84) verifies the postconditions before `action-complete`. [src/discovery/run.ts:23](/Users/emiliosanchez-harris/interface-computer-use/src/discovery/run.ts:23) independently rechecks the postconditions and ownership before calling `Recorder.completed`. [src/recording/recorder.ts:75](/Users/emiliosanchez-harris/interface-computer-use/src/recording/recorder.ts:75) appends the executed step. The artifact stores `amountCents` plus its transform, not the original `25.00` string. Screen transitions, exception rules and completion checks are developer-authored; the action choice is what the model supplied.

Strongest existing loop test: **“A02 A05 A17 A18 development fixture records real UI actions and replays changed inputs”**, [tests/integration/slice.test.ts:20](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/slice.test.ts:20). It verifies actual browser execution, symbolic output and changed invocation, but uses a disclosed fixture model. Pair it with the historical live step above for genuine-discovery evidence. Boundary test: **“discovery stale candidate terminates within bounded calls”**, generated at [tests/integration/discovery-errors.test.ts:23](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/discovery-errors.test.ts:23), expects `STALE_CANDIDATE`, one decision and no search request.

**Question:** What would stop a valid-looking model proposal from becoming an unsafe or falsely recorded step?

Answer checkpoints: strict Decision schema; current-observation candidate membership; fresh strict target/effect checks; host policy and ownership checks before dispatch; verified postconditions before recorder append. A syntactically valid proposal does not grant permission.

**17–24 minutes: changed-input replay and no model path**

Open [src/cli/replay.ts:15](/Users/emiliosanchez-harris/interface-computer-use/src/cli/replay.ts:15), then [src/replay/run.ts:20](/Users/emiliosanchez-harris/interface-computer-use/src/replay/run.ts:20). The CLI reads the requested file and invocation. Replay validates both before creating a browser and hashes the original artifact string. `Runtime.create` at [src/session/browser.ts:50](/Users/emiliosanchez-harris/interface-computer-use/src/session/browser.ts:50) creates a fresh browser/context/page and navigates to `/app`. It does not restore the discovery browser's state.

The replay loop at line 35 installs each step's resume anchor, checks a stable UI, and passes the saved operation to the shared executor. [src/surface/target.ts:6](/Users/emiliosanchez-harris/interface-computer-use/src/surface/target.ts:6) resolves exact role/name or table-relative targets anew in Workspace. Input expressions are resolved as literal values, not executable selector fragments. In this live artifact, M-207 determines the member fill, and 3750 cents becomes `37.50`; account outputs happen to match because the app's new member defaults match the request, as established above.

After all six steps, [src/session/resume.ts:38](/Users/emiliosanchez-harris/interface-computer-use/src/session/resume.ts:38) calls `extract` and checks the same ownership epoch before returning. [src/surface/checks.ts:166](/Users/emiliosanchez-harris/interface-computer-use/src/surface/checks.ts:166) reads visible table cells with unique labels; lines 189–207 parse money/output types and compare every requested business field. The result is derived from the current UI, not saved discovery outputs or a server-state oracle. Existing clean changed-input evidence: [evidence/f15c79d6-b552-4641-ae71-3d706ee05bca/manifest.json:1](/Users/emiliosanchez-harris/interface-computer-use/evidence/f15c79d6-b552-4641-ae71-3d706ee05bca/manifest.json:1) binds the live hash, fresh source and zero model calls/reserved tokens.

There is no model parameter on `ReplayOptions` and no provider import in this dependency path. Strongest existing runtime test: **“A03 keyless CLI replay runs with a fatal model import hook and immutable artifact”**, [tests/integration/safety.test.ts:34](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/safety.test.ts:34). It removes credentials, runs the real CLI in a child with [scripts/model-bomb.ts:3](/Users/emiliosanchez-harris/interface-computer-use/scripts/model-bomb.ts:3), checks a changed output and zero commits, and hashes the file again. Complement: **“A03 replay dependency graph excludes all discovery provider and fixture modules”**, [tests/unit/architecture.test.ts:6](/Users/emiliosanchez-harris/interface-computer-use/tests/unit/architecture.test.ts:6), traverses the actual source graph and rejects dynamic imports/requires. A zero counter alone would be weaker evidence than these two checks.

Artifact qualification uses [tests/artifact-path.ts:1](/Users/emiliosanchez-harris/interface-computer-use/tests/artifact-path.ts:1). Its `??` fallback applies only when the environment variable is absent; an explicit missing path fails when read. The prior 31-test live qualification set this variable. Other fixture-only tests remain fixture-only. The owner demo's `--require-live` checks at [src/cli/demo.ts:18](/Users/emiliosanchez-harris/interface-computer-use/src/cli/demo.ts:18) prevent its otherwise visible fixture fallback.

**Question:** How do we know a successful replay used the new invocation and could not silently ask the model for help?

Answer checkpoints: input expressions evaluated at execution; output independently extracted and compared; exact artifact hash; isolated fresh context; provider-free dependency graph plus failing import hook and removed credentials. Remember the account-default caveat.

**24–30 minutes: timeout after a possible effect**

Read [src/surface/executor.ts:46](/Users/emiliosanchez-harris/interface-computer-use/src/surface/executor.ts:46) through line 97. The Playwright operation has a real timeout. When it throws `TimeoutError`, the executor first checks policy/ownership and then waits for the step's declared postconditions. If the review transition already happened and its conditions hold, execution can reconcile it. If they do not hold, the run fails. There is no second click dispatch in the catch block. The normal path also verifies conditions before emitting completion.

At [src/surface/checks.ts:128](/Users/emiliosanchez-harris/interface-computer-use/src/surface/checks.ts:128), condition waits retry observation until a deadline. A still-loading screen produces `LOAD_TIMEOUT`; a different unmet checkpoint produces `CHECKPOINT_MISMATCH`. [src/surface/checks.ts:42](/Users/emiliosanchez-harris/interface-computer-use/src/surface/checks.ts:42) can retry observation when navigation destroys the old execution context. These are observation retries, not repeated business effects. Defaults are 5000 ms/action and 10000 ms/condition at [src/session/browser.ts:47](/Users/emiliosanchez-harris/interface-computer-use/src/session/browser.ts:47); do not mistake those separate deadlines for proof of instantaneous cancellation.

Strongest effect-count tests: **“A07 delayed review 125ms recovers without repeating the click”** and the 200ms variant, [tests/integration/outcomes.test.ts:62](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/outcomes.test.ts:62), assert one review POST and recovery. **“A07 exhausted loading deadline stops with one effect”**, line 86, asserts timeout and still one POST. These test delayed UI recovery, not every possible timeout-after-dispatch condition. The actual timed-out-click/handoff case is **“A14 a real timed-out Playwright click cannot fire after HUMAN is granted”**, [tests/integration/review-regressions.test.ts:114](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/review-regressions.test.ts:114): an overlay blocks the click; takeover waits; removing it after HUMAN cannot trigger a late search. That regression uses the development fixture and is not a new live-artifact run.

**Question:** A click reports a timeout. What evidence would justify continuing, and why is clicking again unsafe?

Answer checkpoints: timeout does not establish that no effect occurred; verify declared postconditions and retain ownership/policy checks; stop if state is unproven; never infer cancellation from a timer or retry the effect just because waiting failed.

**30–38 minutes: ownership, outstanding work and resume**

[src/session/controller.ts:40](/Users/emiliosanchez-harris/interface-computer-use/src/session/controller.ts:40) registers the actual underlying operation promise in `pending`. `takeover` at line 64 changes AUTOMATION → PAUSING, increments the epoch to invalidate stale work, and waits for all pending promises to settle. The race's settlement deadline produces a terminal failure; it does not grant HUMAN. HUMAN is emitted only after settlement succeeds. The runtime's settle limit is action timeout plus 1000 ms, while intervention waiting defaults to ten minutes ([src/session/browser.ts:63](/Users/emiliosanchez-harris/interface-computer-use/src/session/browser.ts:63)).

Asynchronous target preparation may be outside `pending`, so the executor checks the captured epoch again immediately before dispatch. Model work is also not a cancellable browser operation: the discovery loop rejects a late response after an epoch change at [src/discovery/run.ts:100](/Users/emiliosanchez-harris/interface-computer-use/src/discovery/run.ts:100). This prevents its proposal from mutating a human-owned page; it does not undo the already-started model request.

On `resume`, [src/session/controller.ts:106](/Users/emiliosanchez-harris/interface-computer-use/src/session/controller.ts:106) enters RESUMING and calls a saved validation function. [src/session/resume.ts:6](/Users/emiliosanchez-harris/interface-computer-use/src/session/resume.ts:6) reobserves both step preconditions and postconditions: only-before means repeat that not-yet-completed step; only-after means continue to the next; overlapping click anchors are ambiguous and rejected. An idempotent fill/select whose postcondition already holds can advance. A review postcondition additionally triggers full output extraction. If nothing matches, ownership returns to HUMAN with `RESUME_STATE_MISMATCH`. This live artifact's anchors include member and entered amount, but no account-value anchor; its final output check remains the account guard.

For expiry during “Prepare transfer,” the same page/context survives. `intervene` saves a restricted snapshot and waits for operator restoration. The safe postcondition after restoring can place replay at the next step without pretending a human action was learned. Discovery has the additional `completedDispatches` check at [src/discovery/run.ts:33](/Users/emiliosanchez-harris/interface-computer-use/src/discovery/run.ts:33) before it records a resumed action.

Strongest existing end-to-end test: **“A13 same-session expiry handoff and test-operator resumption”**, [tests/integration/handoff.test.ts:32](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/handoff.test.ts:32), using the selected artifact and recording `test_operator`. Rejection test: **“A15 wrong restored member cannot resume”**, line 63, changes member and asserts HUMAN remains plus zero review requests. The in-flight invariant is directly exercised by **“A14 human ownership waits for the actual underlying action and rejects queued stale work”**, [tests/unit/ownership.test.ts:22](/Users/emiliosanchez-harris/interface-computer-use/tests/unit/ownership.test.ts:22), and the real overlay-click regression above. Finalization race test: **“A14 takeover during final completion cannot finalize or close the human-owned page”**, [tests/integration/review-regressions.test.ts:53](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/review-regressions.test.ts:53), covers a fixture-only race after the last action.

**Question:** Why is `await Promise.race([action, timeout])` insufficient to declare that the human owns the browser, and what must resume prove?

Answer checkpoints: losing promises keep running; track and settle actual effects, block new scheduling and invalidate epochs; reobserve a unique declared continuation, including member/form/output checks; retain HUMAN if continuation is unsafe. Ownership is cooperative, not an OS mouse/keyboard lock.

**38–44 minutes: safety, redaction and the limits of each**

| Boundary | Source to follow | Actual protection and limit |
|---|---|---|
| Untrusted model/artifact | [src/domain/actions.ts:64](/Users/emiliosanchez-harris/interface-computer-use/src/domain/actions.ts:64), [src/domain/artifact.ts:72](/Users/emiliosanchez-harris/interface-computer-use/src/domain/artifact.ts:72) | Closed primitive actions/expressions and exact trusted profile/completion contract; no artifact-authored JavaScript or arbitrary policy module. Parsing does not prove parameter completeness, as the account finding demonstrates. |
| Semantic permission | [src/policy/profile.ts:82](/Users/emiliosanchez-harris/interface-computer-use/src/policy/profile.ts:82), [src/surface/target.ts:50](/Users/emiliosanchez-harris/interface-computer-use/src/surface/target.ts:50) | Inspect actual tag/type/form action/method or href; check known effect before dispatch. A safe label alone is insufficient. Designed for this curated UI, not arbitrary pages. |
| Browser network effects | [src/session/browser.ts:70](/Users/emiliosanchez-harris/interface-computer-use/src/session/browser.ts:70), [src/policy/profile.ts:18](/Users/emiliosanchez-harris/interface-computer-use/src/policy/profile.ts:18) | Exact loopback origin plus route/method allowlist, still active during HUMAN. `/commit` is absent. Service workers are blocked; popups/downloads rejected; native dialogs dismissed and marked failure. This is not a general OS/network sandbox or a validator of every query parameter's meaning. |
| Current UI vs stored evidence | [src/surface/observe.ts:59](/Users/emiliosanchez-harris/interface-computer-use/src/surface/observe.ts:59), [src/surface/snapshot.ts:35](/Users/emiliosanchez-harris/interface-computer-use/src/surface/snapshot.ts:35) | Discovery intentionally receives bounded visible business values; password controls are excluded. Persisted snapshots replace all control values and unknown labels, retain route templates and structural counts. A redacted evidence file does not mean nothing sensitive could ever appear in a model observation. |
| Event/result writing | [src/evidence/schema.ts:5](/Users/emiliosanchez-harris/interface-computer-use/src/evidence/schema.ts:5), [src/evidence/store.ts:75](/Users/emiliosanchez-harris/interface-computer-use/src/evidence/store.ts:75), [src/evidence/store.ts:106](/Users/emiliosanchez-harris/interface-computer-use/src/evidence/store.ts:106) | Strict schemas allow only bounded metadata and reason/code enums; raw messages are rejected. Saved success outputs become `[REDACTED]`. Mandatory write failure stops the run; it is not silently ignored. |
| Caller and human provenance | [src/cli/replay.ts:27](/Users/emiliosanchez-harris/interface-computer-use/src/cli/replay.ts:27), [src/session/browser.ts:97](/Users/emiliosanchez-harris/interface-computer-use/src/session/browser.ts:97) | Caller stdout still contains complete requested synthetic outputs. Operator telemetry records field category/action, not field contents. Actor selection is configuration, not proof of a person's identity. Do not publish raw caller logs or treat `actor:human` alone as owner attestation. |

Strongest safety test: **“A11 semantic final-submit proposal and human route request are blocked”**, [tests/integration/safety.test.ts:81](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/safety.test.ts:81), checks denial and the independent server commit counter. Also read **“A11 button-specific formaction override is checked before activation”**, line 157, for why route inspection matters. Strongest redaction test: **“A16 sensitive field text URL and human changes never reach restricted persistence”**, [tests/integration/redaction.test.ts:33](/Users/emiliosanchez-harris/interface-computer-use/tests/integration/redaction.test.ts:33), plants a sentinel in the UI, URL and password field, then inspects every saved file. **“A16 model explanations and exception messages cannot enter logs or artifacts”**, line 80, covers raw error/prose rejection. These are fixture-based mechanism tests, not human evidence or universal data-loss-prevention claims.

**Question:** Which layer would stop a harmlessly labeled control wired to `/commit`, and what information can still exist outside the sanitized evidence files?

Answer checkpoints: inspect real control effect before dispatch, then independently enforce context routing; exact origin alone is insufficient; model observations and caller stdout can contain business data; automatic evidence is allowlisted and redacted, not a complete privacy boundary for the computer.

**44–45 minutes: owner acceptance record**

Do not mark your review complete just because these notes exist. State which questions you can explain and which remain unclear. Manual operation is a separate check and can happen after the 45-minute walkthrough.

When you choose to perform the owner handoff, use an interactive terminal/display:

```bash
npm run demo:handoff -- --require-live
```

Enter `status` and confirm HUMAN. In the same browser use fake `demo-only` in Demo password, click Restore session, and wait for Prepare transfer / M-207. Stop browser interaction, then enter `resume`. Verify CHK-207 → SAV-207, 37.50 USD, fee 0.00, USD, AWAITING_CONFIRMATION, and terminal success / commits: 0. This still exercises the default direction; it does not repair or retest the reverse-direction finding.

When reporting your actual result, include the run ID or `Evidence: .runs/demo/<run-id>` line, what you did, the terminal outcome, and what you saw. The command and evidence output locations are implemented at [src/cli/demo.ts:39](/Users/emiliosanchez-harris/interface-computer-use/src/cli/demo.ts:39) and [src/cli/demo.ts:57](/Users/emiliosanchez-harris/interface-computer-use/src/cli/demo.ts:57); terminal controls at [src/cli/operator.ts:10](/Users/emiliosanchez-harris/interface-computer-use/src/cli/operator.ts:10). I will inspect that specific run's manifest, exact artifact hash, source/dirty state, ownership events and safe result, then record **owner-reported observation** separately from **automatically recorded evidence**. The commit counter is printed by the demo; it is not a field in the safe manifest. Missing logs or observations remain gaps. An automated `test_operator` run will never be relabeled as yours, and a mismatch between your report and recorded events will be retained rather than silently resolved.
